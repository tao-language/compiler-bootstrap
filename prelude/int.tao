: Int, Int -> Int
x + y = %int_add(x, y)

: Int, Int -> Int
x - y = %int_sub(x, y)

: Int, Int -> Int
x * y = %int_mul(x, y)

: Int, Int -> Num
x / y = %int_div(x, y)

: Int, Int -> Int
x // y = %int_divi(x, y)

: Int, Int -> Int
x ^ y = %int_pow(x, y)
