import prelude/bool {Bool}

// Function overload on module name
fn (and) {
| bool._and(Bool, Bool)
}

// Let definition (equivalent form; not used because the `fn (and)`
// overload above already defines the name `and`):
// let (and) = bool._and

>>> True and False  False
