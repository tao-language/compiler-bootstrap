// Comptime & Type-Level Programming
// Verifies %comptime. Ensures the language can perform computation
// at compile-time to generate types or terms.

// Compute a value at comptime to use in a term
let size = %comptime %call prim.add(10, 5);

// Use the computed value (should be normalized to 15)
size
