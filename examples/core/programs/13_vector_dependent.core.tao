// Vector with Dependent Types (Length-Indexed)
// A vector where the length is part of the type

// Vector of length 2 containing values
// #VCons takes: element, length index, tail vector
#VCons(42, #Zero, #VCons(43, #Zero, #VNil))
