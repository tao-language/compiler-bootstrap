// Vector with Dependent Types (Golden Sample)
// Note: Length-indexed vectors require dependent type support

// For now, use Church encoding
// nil = λc. λn. n
// cons = λh. λt. λc. λn. c h t
let nil = c -> n -> n in
let cons = h -> t -> c -> n -> ((c)(h))(t) in
((cons)(1))(((cons)(2))(nil))
