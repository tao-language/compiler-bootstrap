// Pattern Matching Example (Golden Sample)
// Note: Match expression parsing needs refinement for multiple cases
// This demonstrates the intended syntax

// Intended syntax (not yet fully working):
// %match x ~ $Type {
//   | #True -> 1
//   | #False -> 0
// }

// For now, use if-then-else or direct application
let x = #True in x
