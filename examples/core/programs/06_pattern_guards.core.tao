// Pattern Guards (Golden Sample)
// This demonstrates the syntax for pattern guards

// Match with guards (requires prim operations)
let n = 5 in %match n ~ $Type { | k ? (k == 0) -> #True | _k -> #False }
