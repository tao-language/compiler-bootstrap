// Pattern Guards Example
// Demonstrates guard conditions in match expressions
// Note: Full guard support requires parsing fixes

// Simple example without guards (works)
let n = 5 in
%match n ~ $Type {
  | 0 -> 1
  | _k -> 0
}
