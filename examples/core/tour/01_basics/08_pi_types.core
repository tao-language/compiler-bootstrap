// Pi is a dependently typed type.
// The domain is passed with parentheses.
// The codomain comes after an arrow (->).
// The input variable can then be accessed in the codomain (body).
%pi(x: %Type) -> x
