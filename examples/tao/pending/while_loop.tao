// While loop: condition-based iteration
// Note: while loop syntax not yet implemented, using recursive function instead
// Expected output: 0 (after counting down from 5)

fn countdown(count: I32) -> I32 {
  match count > 0 {
    | true -> countdown(count - 1)
    | false -> count
  }
}
countdown(5)
