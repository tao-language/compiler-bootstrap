// Tao Example: Logical Operators
// Demonstrates && and || with correct precedence

// && has higher precedence than ||
// true || false && false  means  true || (false && false)  which is  true
1 || 0 && 0

// Parentheses for clarity  
(1 || 0) && 0

// NOT operator
!0

// Combined
!0 && 1 || 0
