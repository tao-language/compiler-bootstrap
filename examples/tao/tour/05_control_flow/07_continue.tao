// Used to skip to the next iteration.
// Can be used on both for loops and while loops.
fn continue_example() -> Int {
  let mut sum = 0
  for i in [1, 2, 3, 4, 5, 6] {
    if i == 5 {
      continue
    }
    mut sum += i
  }
  return sum
}
> continue_example() ~> 16  // 1 + 2 + 3 + 4 + 6
