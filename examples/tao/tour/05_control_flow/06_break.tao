// Used to stop a loop early.
// Can be used on both for loops and while loops.
fn break_example() -> Int {
  let mut sum = 0
  for i in [1, 2, 3, 4, 5, 6] {
    if i == 5 {
      break
    }
    mut sum += i
  }
  return sum
}
> break_example() ~> 10  // 1 + 2 + 3 + 4
