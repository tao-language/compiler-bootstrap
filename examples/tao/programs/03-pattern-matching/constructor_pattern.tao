// Constructor pattern: Custom type
// Expected output: 42
// Note: Type definitions are not yet supported in Tao.
// This example uses wildcard pattern instead.
// TODO: Update when type definitions are added.

let some_value = 42
match some_value {
  | x -> x
}
