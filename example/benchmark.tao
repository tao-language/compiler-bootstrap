bench : [Text] -> IO (Number, Number)
bench cmd =
  start_time <- IO.now
  process <- IO.run cmd
  _ <- process.wait
  end_time <- IO.now
  memory <- process.used.memory.max
  return (end_time - start_time, memory)

bench
