// Constructor Application
// Constructors are applied like functions: #Tag(argument)
// Note: Constructors must be defined in the type context
// This example uses a simple constructor to demonstrate the syntax
// Full ADT support requires type definitions (coming soon)

// For now, use a simple wrapper pattern
%let wrap = x -> x in
wrap(42)
