import gleam/list
import gleam/regexp.{type Regexp}
import gleam/string

pub fn test_(path_re: Regexp, pattern_re: Regexp) {
  // filenames: List(String)
  // modules: List(#(String, Block))
  todo
}
